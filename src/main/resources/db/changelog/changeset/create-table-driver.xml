<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog>
    <changeSet id="Create driver" author="Second Employee">
        <sql>
            create table driver (
            id int8 generated by default as identity,
            busy boolean DEFAULT false,
            car varchar(255),
            dayoff varchar(255),
            name varchar(255),
            workstatus boolean DEFAULT true,
            primary key (id)
            )
        </sql>
    </changeSet>
</databaseChangeLog>